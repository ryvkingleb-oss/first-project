<%- include('partials/head', { title }) %>
<h1><%= title %></h1>
<p class="lede"><%= message %></p>
<p class="actions">
  <a class="btn" href="<%= user ? HOME[user.role] : '/' %>"><%= user ? 'В кабинет' : 'На главную' %></a>
</p>
<%- include('partials/foot') %>
