{
  "name": "first-project (static site)",
  "terminals": [
    {
      "name": "web-server",
      "command": "python3 -m http.server 3000 --bind 0.0.0.0",
      "description": "Serves the static site (index.html / style.css) at http://localhost:3000"
    }
  ],
  "ports": [
    {
      "name": "web server",
      "port": 3000
    }
  ]
}
